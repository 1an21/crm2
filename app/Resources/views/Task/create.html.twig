{% extends '::base.html.twig' %}

{% block body %}

        {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" class="btn btn-default btn-sm" style="float: right;" value="Add" />
        <a href="{{ path('all') }}" class="btn btn-info btn-sm" style="float: left;">Back to All</a>
        {{ form_end(form) }}
{% endblock %}