{% extends '::base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-default btn-sm" style="float: right;" value="Edit" />
    {{ form_end(form) }}
{% endblock %}